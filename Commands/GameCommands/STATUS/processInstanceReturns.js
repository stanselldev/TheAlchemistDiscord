export async function processInstanceReturns(msg, inventory) {
    // Checks the instanceData array and runs some calculations to determine
        // what drops/returns/items the user gets from the just completed instance
    

    // Update inventory and then return it as an object

    
    return {
        test: test
    }
}